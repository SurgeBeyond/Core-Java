package encapsulation.implementinginterface;

interface codable{
	public void syntax();
}

public class InterfaceIntro implements codable{
	public void syntax() {} //Interface ensures methods implementation but not the code quality
}
